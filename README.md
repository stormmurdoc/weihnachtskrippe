# Arduino Weihnachtskrippe

## How to build this project

    # Build project
    > platformio run

    # Upload firmware
    > platformio run --target upload

    # Clean build files
    > platformio run --target clean
